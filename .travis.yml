language: python

python:

  - "3.5"

# command to install dependencies

install:

    - pip install geatpy

# command to run tests, we only need to test the installation so that we do nothing here. And Geatpy can't work on Linux. So we don't need to test here.

script:

    - 
